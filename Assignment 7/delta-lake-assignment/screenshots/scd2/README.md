# MERGE screenshot

Run the MERGE cell in `notebooks/delta_scd_assignment.ipynb` and capture a
screenshot that includes the `MERGE completed successfully` output. Save the
image in this folder as `merge_completed.png`.

This folder documents the incremental update/insert step. The notebook uses a
Delta Lake SCD Type 1-style MERGE: matching customer IDs are updated and new
customer IDs are inserted.
